apiVersion: v1
kind: ServiceAccount
metadata:
  name: helloworld-sa
  namespace: default
  annotations:
    # This annotation is the key to IRSA — it tells EKS which IAM role this service account can assume
    eks.amazonaws.com/role-arn: arn:aws:iam::YOUR_ACCOUNT_ID:role/helloworld-secrets-role
