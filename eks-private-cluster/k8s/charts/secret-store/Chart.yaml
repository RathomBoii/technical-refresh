apiVersion: v2
name: secret-store
description: Configures ESO resources (ClusterSecretStore, ExternalSecret, IRSA ServiceAccount). ESO operator is installed separately via the external-secrets-operator ArgoCD app.
type: application
version: 1.0.0
appVersion: "v0.5.0"
