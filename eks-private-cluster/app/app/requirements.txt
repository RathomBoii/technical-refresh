# fastapi pinned <0.137: 0.137.0 stores internal `_IncludedRouter` wrapper
# objects (no `.path`) in app.routes, which crashes prometheus-fastapi-instrumentator
# (<=8.0.0) -> AttributeError -> HTTP 500 on every request, incl. /health.
# Remove the cap once the instrumentator ships a fix. See:
# https://github.com/trallnag/prometheus-fastapi-instrumentator/issues/370
fastapi==0.136.3
uvicorn
psycopg2-binary
email-validator

# Phase 1 — Metrics: exposes /metrics endpoint for Prometheus scraping
prometheus-fastapi-instrumentator==8.0.0

# Phase 3 — Tracing: OpenTelemetry SDK + FastAPI/psycopg2 auto-instrumentation + OTLP gRPC export
opentelemetry-sdk
opentelemetry-api
opentelemetry-exporter-otlp-proto-grpc
opentelemetry-instrumentation-fastapi
opentelemetry-instrumentation-psycopg2

# Phase 4 — Logging: structured JSON logs with trace_id injection
python-json-logger